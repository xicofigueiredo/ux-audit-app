<!DOCTYPE html>
<html>
  <head>
    <title>Projects - UX Audit App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <%= stylesheet_link_tag "application" %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="flex h-screen bg-gray-100">
      <!-- Sidebar -->
      <%= render 'shared/app_sidebar' %>
  <!-- Main Content -->
  <div class="flex-1 flex flex-col overflow-hidden">
    <!-- Top Bar -->
    <header class="flex justify-between items-center px-6 py-4 bg-white border-b shadow-sm">
      <div class="flex items-center space-x-4">
        <div class="relative">
          <button class="flex items-center px-4 py-2 text-gray-700 bg-gray-100 rounded-md text-sm font-medium focus:outline-none focus:ring-2 focus:ring-purple-600">
            Personal Workspace
            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
          </button>
          <!-- Dropdown would go here -->
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <div class="relative">
          <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.465 6.022 6 8.034 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path></svg>
          <span class="absolute top-0 right-0 block h-2 w-2 transform -translate-y-1/2 translate-x-1/2 rounded-full bg-purple-600"></span>
        </div>
        <div class="relative">
          <button class="flex items-center focus:outline-none rounded-full">
            <span class="sr-only">User menu</span>
             <img class="h-8 w-8 rounded-full" src="https://via.placeholder.com/150" alt="User Avatar">
            <svg class="w-4 h-4 ml-2 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
          </button>
          <!-- Dropdown would go here -->
        </div>
      </div>
    </header>
    <!-- Main Content Area -->
    <main class="flex-1 overflow-y-auto p-8">
      <div class="flex justify-between items-center mb-8">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">Projects</h1>
          <p class="text-gray-600">Manage your UX audits and reports</p>
        </div>
        <%= link_to video_audits_path, class: "bg-purple-600 hover:bg-purple-700 text-white font-semibold px-6 py-3 rounded-lg shadow-md transition flex items-center" do %>
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path></svg>
          New Audit
        <% end %>
      </div>

      <!-- Projects Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% @audits.each do |audit| %>
          <div class="bg-white rounded-lg shadow overflow-hidden">
            <!-- Thumbnail Image or Placeholder -->
            <div class="w-full h-48 bg-gray-200 flex items-center justify-center text-gray-500 overflow-hidden">
              <% if audit.thumbnail_image.present? %>
                <img src="data:image/jpeg;base64,<%= audit.thumbnail_image %>"
                     alt="Audit thumbnail"
                     class="w-full h-full object-cover"
                     loading="lazy">
              <% else %>
                <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
              <% end %>
            </div>
            <div class="p-6">
              <div class="flex justify-between items-center mb-4">
                <h3 class="text-lg font-bold text-gray-900">
                  <% 
                    # Try to get the workflow title from the LLM response
                    workflow_title = nil
                    if audit.llm_response.present?
                      if audit.llm_response.is_a?(String)
                        begin
                          parsed_response = JSON.parse(audit.llm_response)
                          workflow_title = parsed_response.dig('workflowSummary', 'workflowtitle')
                        rescue JSON::ParserError
                          # If JSON parsing fails, try to extract from string
                          workflow_title = nil
                        end
                      elsif audit.llm_response.is_a?(Hash)
                        workflow_title = audit.llm_response.dig('workflowSummary', 'workflowtitle')
                      end
                    end
                    
                    # Display the title with fallbacks
                    if workflow_title.present?
                      workflow_title
                    elsif audit.video.present? && audit.video.filename.present?
                      truncate(audit.video.filename.to_s, length: 30)
                    else
                      "Untitled Project"
                    end
                  %>
                </h3>
                <!-- Options dropdown -->
                <div class="relative" data-dropdown>
                  <button type="button" class="text-gray-400 hover:text-gray-600 dropdown-toggle" data-dropdown-toggle="dropdown-<%= audit.id %>">
                     <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 6a2 2 0 110-4 2 2 0 010 4zM10 12a2 2 0 110-4 2 2 0 010 4zM10 18a2 2 0 110-4 2 2 0 010 4z"></path></svg>
                  </button>
                  <div id="dropdown-<%= audit.id %>" class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg z-10 hidden">
                    <div class="py-1">
                      <%= link_to video_audit_path(audit), class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" do %>
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg>
                        View Details
                      <% end %>
                      <%= button_to video_audit_path(audit), method: :delete,
                          data: { turbo_confirm: "Are you sure you want to delete this project? This action cannot be undone." },
                          class: "w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-red-50",
                          form: { class: "inline" } do %>
                        <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                        Delete Project
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
              <div class="flex items-center space-x-2 mb-4">
                <% if audit.status == 'completed' %>
                  <span class="inline-block bg-green-100 text-green-800 text-xs font-medium px-2.5 py-0.5 rounded-full">completed</span>
                <% elsif audit.status == 'processing' %>
                  <span class="inline-block bg-yellow-100 text-yellow-800 text-xs font-medium px-2.5 py-0.5 rounded-full">processing</span>
                <% else %>
                   <span class="inline-block bg-gray-100 text-gray-800 text-xs font-medium px-2.5 py-0.5 rounded-full"><%= audit.status %></span>
                <% end %>
                 <span class="text-sm text-gray-500">Created <%= time_ago_in_words(audit.created_at) %> ago</span>
              </div>
              <div class="flex justify-end">
                 <%= link_to "View Report", video_audit_path(audit), class: "text-purple-600 hover:underline text-sm font-semibold" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>

    </main>
  </div>
</div>

<style>
/* Mobile responsive styles for projects page */
@media (max-width: 768px) {
  .flex.h-screen {
    flex-direction: column;
    height: auto;
  }

  .w-64 {
    width: 100%;
    box-shadow: none;
    border-bottom: 1px solid #e5e7eb;
    padding: 1rem 0;
  }

  .w-64 .flex.items-center {
    padding: 0 1rem;
    margin-bottom: 1rem;
  }

  .w-64 nav {
    padding: 0 1rem;
    display: flex;
    flex-direction: row;
    gap: 0.5rem;
    overflow-x: auto;
    padding-bottom: 0.5rem;
  }

  .w-64 nav a,
  .w-64 nav span {
    white-space: nowrap;
    min-width: auto;
    padding: 8px 16px;
    font-size: 0.875rem;
  }

  .w-64 .border-t {
    display: none;
  }

  .flex-1.flex.flex-col {
    padding: 0;
  }

  .flex-1.flex.flex-col header {
    padding: 1rem;
  }

  .flex-1.flex.flex-col main {
    padding: 1rem;
  }

  .grid.grid-cols-1.sm\\:grid-cols-2.lg\\:grid-cols-3 {
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .flex.justify-between.items-center.mb-8 {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .flex.justify-between.items-center.mb-8 div:first-child {
    width: 100%;
  }

  .flex.justify-between.items-center.mb-8 a {
    width: 100%;
    text-align: center;
    justify-content: center;
  }
}

@media (max-width: 480px) {
  .w-64 .flex.items-center .text-xl {
    font-size: 1.25rem;
  }

  .w-64 nav {
    gap: 0.25rem;
  }

  .w-64 nav a,
  .w-64 nav span {
    padding: 6px 12px;
    font-size: 0.8rem;
  }

  .flex-1.flex.flex-col header {
    padding: 0.75rem;
  }

  .flex-1.flex.flex-col main {
    padding: 0.75rem;
  }
}
</style>

<script nonce="<%= content_security_policy_nonce %>">
// Dropdown functionality
document.addEventListener('DOMContentLoaded', function() {
  const dropdownToggles = document.querySelectorAll('.dropdown-toggle');

  dropdownToggles.forEach(toggle => {
    toggle.addEventListener('click', function(e) {
      e.stopPropagation();
      const dropdownId = this.getAttribute('data-dropdown-toggle');
      const dropdown = document.getElementById(dropdownId);

      // Close all other dropdowns
      document.querySelectorAll('[id^="dropdown-"]').forEach(otherDropdown => {
        if (otherDropdown.id !== dropdownId) {
          otherDropdown.classList.add('hidden');
        }
      });

      // Toggle current dropdown
      dropdown.classList.toggle('hidden');
    });
  });

  // Close dropdowns when clicking outside
  document.addEventListener('click', function() {
    document.querySelectorAll('[id^="dropdown-"]').forEach(dropdown => {
      dropdown.classList.add('hidden');
    });
  });

  // Prevent dropdown from closing when clicking inside it
  document.querySelectorAll('[id^="dropdown-"]').forEach(dropdown => {
    dropdown.addEventListener('click', function(e) {
      e.stopPropagation();
    });
  });
});
</script>
    </div>
  </body>
</html> 